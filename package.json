{
  "name": "llc_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "consume": "npm run build && node ./dist/consume.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/puppeteer": "^3.0.1",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "puppeteer": "^5.2.1"
  }
}
